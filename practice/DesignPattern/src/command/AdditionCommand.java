package command;

public class AdditionCommand implements Command {
    @Override
    public double execute() {
        return 0;
    }

    @Override
    public double undo() {
        return 0;
    }
}
