package command;

public class SubtractionCommand implements Command {
    @Override
    public double execute() {
        return 0;
    }

    @Override
    public double undo() {
        return 0;
    }
}
